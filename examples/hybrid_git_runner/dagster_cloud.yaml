locations:
  - location_name: hybrid_git_runner
    code_source:
      module_name: hybrid_git_runner.definitions
    # agent_queue routes this location to a Hybrid agent listening on
    # that queue — keeps this location off any Serverless agents in
    # the same deployment.
    agent_queue: hybrid-test
