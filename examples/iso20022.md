# ISO 20022 Payment Parser — synthetic XML → flat transactions DataFrame
> ✅ **Dagster+ Serverless / Hybrid:** deploys as-is via [`deploy_to_dagster_plus.sh`](deploy_to_dagster_plus.sh).

**Validated end-to-end** (pure Python). 10 alternating pacs.008 (credit transfer) + pacs.002 (status report) ISO 20022 XML messages → 10 flat transaction rows ready for warehouse / reconciliation.

```
iso20022_messages       ← synthetic_data_generator (iso20022_payments, 10 msgs)
       │
       └── payments_flat   ← iso20022_payment_parser
```

## Components used

| Component | What it does |
|---|---|
| `synthetic_data_generator` | `schema_type: iso20022_payments` alternates between pacs.008 (FI-to-FI Customer Credit Transfer) and pacs.002 (Payment Status Report). Realistic IBANs, BICs, currencies. |
| `iso20022_payment_parser` | Stdlib `xml.etree` parser. Auto-detects message type from XML namespace, strips namespaces for portable XPaths, runs the matching extractor. |

## Live output

```
10 rows
By message_type:
  pacs.008    5
  pacs.002    5
```

**pacs.008 sample (credit transfers):**

| msg_id | amount | currency | debtor_name | creditor_name |
|---|---|---|---|---|
| MSG00000001 | 20,806.27 | GBP | Globex Industries | Pied Piper Ltd |
| MSG00000003 | 37,761.14 | USD | Initech LLC | Pied Piper Ltd |
| MSG00000005 | 40,572.10 | USD | Globex Industries | Pied Piper Ltd |

**pacs.002 sample (status reports):**

| msg_id | status | reason_code |
|---|---|---|
| MSG00000002 | ACCP | — |
| MSG00000004 | ACCP | — |
| MSG00000006 | ACSC | — |

Status codes follow ISO 20022 standard: `ACCP` (accepted-pending settlement), `ACSC` (accepted-settlement complete), `RJCT` (rejected — `reason_code` populated), `PDNG` (pending).

## Supported message families

| ID | Name | Output |
|---|---|---|
| `pacs.008` | FI-to-FI Customer Credit Transfer | One row per `CdtTrfTxInf` |
| `pain.001` | Customer Credit Transfer Initiation | One row per transaction |
| `pacs.002` | Payment Status Report | One row per status |
| `camt.054` | Bank-to-Customer Debit/Credit Notification | One row per entry |

Other families fall back to a generic header-only extractor.

## Why ISO 20022 matters

ISO 20022 is replacing legacy SWIFT MT globally. SEPA, Fedwire, CHIPS, and most major clearing networks are on it now. Every fintech / treasury / payments operations pipeline ingests these XML envelopes for reconciliation, anomaly detection, regulatory reporting.

## Wave 2 (parked)

- FIX protocol parser (trading messages)
- ACORD parser (insurance)
- X12 EDI parser (270/271, 837/835, 850/855/856)
- XBRL facts extractor (SEC filings)

## Run

```bash
curl -fsSL https://raw.githubusercontent.com/eric-thomas-dagster/dagster-community-components-cli/main/examples/setup_iso20022_demo.sh | bash
cd iso20022-demo
uv run dg launch --assets '*'
```

## See also

<!-- TODO: link related walkthroughs -->
